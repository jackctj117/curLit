"""Current-events pipeline (CL-6iu7) — GDELT ingestion + LLM impact
assessment over the shared ``geo_events`` table. The consumer half
(confirmation + strategy) lives with the strategy layer and advances
rows past ASSESSED."""
